effect Foo! {
    foo!(a) Int
}

effect Bar {
    bar() Int
}

let h = fn(x) {
    handler {
        return(x) { x }
        bar() { resume(x) }
    }
};

let e = elaboration Foo! -> <> {
    foo!(x) { 
        handle[h(5)] x
     }
};

let main = handle[h(3)] elab foo!(bar());
